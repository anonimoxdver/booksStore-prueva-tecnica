

export const types = {
    LoadCartFromCookies: { type: '[Cart] - LoadCart from cookies | storage' }, 
    AddBook: { type: '[Cart] - Update products in cart' },
    UpdateOrder: { type: '[Cart] - Update order summary' },
    deleteBook: { type: '[Cart] - Remove product in cart' },
    createOrder: { type: '[Cart] - Order complete' }
}